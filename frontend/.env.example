# Base URL of the OBLIGRA backend API, with no trailing slash.
# Local development: http://localhost:8000
# Production: the backend's public HTTPS URL (e.g. an AWS App Runner
# service URL such as https://xxxxxxxxxx.us-east-1.awsapprunner.com, or a
# custom domain pointed at it).
VITE_API_BASE_URL=
